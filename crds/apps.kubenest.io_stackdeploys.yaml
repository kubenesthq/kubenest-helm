---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: stackdeploys.apps.kubenest.io
spec:
  group: apps.kubenest.io
  names:
    kind: StackDeploy
    listKind: StackDeployList
    plural: stackdeploys
    shortNames:
    - stack
    singular: stackdeploy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.stackRef.name
      name: Stack
      type: string
    - jsonPath: .spec.stackRef.version
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyComponents
      name: Ready
      type: string
    - jsonPath: .status.totalComponents
      name: Total
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          StackDeploy is the Schema for the stackdeploys API
          StackDeploy represents a composition of workloads and addons with dependency management
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackDeploySpec defines the desired state of StackDeploy
            properties:
              components:
                description: Components define the workloads and addons in this stack
                items:
                  description: ComponentSpec defines a component within a stack
                  properties:
                    addonSpec:
                      description: AddonSpec is used when Type is "addon"
                      properties:
                        chart:
                          description: Chart defines the Helm chart to deploy
                          properties:
                            name:
                              description: Name is the chart name
                              type: string
                            repo:
                              description: Repo is the Helm repository URL
                              type: string
                            version:
                              description: Version is the chart version
                              type: string
                          required:
                          - name
                          - repo
                          - version
                          type: object
                        createNamespace:
                          default: false
                          description: CreateNamespace indicates if the chart should
                            create its own namespace
                          type: boolean
                        timeout:
                          default: 5m
                          description: Timeout for Helm operations
                          type: string
                        type:
                          default: generic
                          description: |-
                            Type is a hint for export extraction (e.g., postgres, redis, kafka).
                            This is not a constraint - any Helm chart can be deployed.
                          type: string
                        values:
                          description: Values are the Helm values to apply
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - chart
                      type: object
                    dependsOn:
                      description: DependsOn lists the names of components this component
                        depends on
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is the name of the component
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    type:
                      allOf:
                      - enum:
                        - workload
                        - addon
                      - enum:
                        - workload
                        - addon
                      description: Type defines the type of component
                      type: string
                    workloadSpec:
                      description: WorkloadSpec is used when Type is "workload"
                      properties:
                        args:
                          description: Args overrides the default container args
                          items:
                            type: string
                          type: array
                        buildMode:
                          allOf:
                          - enum:
                            - image
                            - dockerfile
                            - buildpack
                          - enum:
                            - image
                            - dockerfile
                            - buildpack
                          default: image
                          description: BuildMode defines how the container image should
                            be obtained
                          type: string
                        builderImage:
                          description: BuilderImage specifies the builder image for
                            buildpack builds
                          type: string
                        chart:
                          description: Chart defines the Helm chart to deploy (optional,
                            defaults to kubenest-workload)
                          properties:
                            name:
                              description: Name is the chart name
                              type: string
                            repo:
                              description: Repo is the Helm repository URL
                              type: string
                            version:
                              description: Version is the chart version
                              type: string
                          required:
                          - name
                          - repo
                          - version
                          type: object
                        command:
                          description: Command overrides the default container command
                          items:
                            type: string
                          type: array
                        env:
                          description: |-
                            Environment variables for the workload
                            Supports value, secretKeyRef, configMapKeyRef, and exportRef
                          items:
                            description: EnvVar represents an environment variable
                              with support for exportRef
                            properties:
                              name:
                                description: Name of the environment variable
                                type: string
                              value:
                                description: Value is the literal value of the environment
                                  variable
                                type: string
                              valueFrom:
                                description: ValueFrom specifies a source for the
                                  environment variable's value
                                properties:
                                  configMapKeyRef:
                                    description: ConfigMapKeyRef selects a key of
                                      a ConfigMap in the pod's namespace
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: |-
                                          Name of the referent.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          TODO: Add other useful fields. apiVersion, kind, uid?
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  exportRef:
                                    description: ExportRef selects a key from another
                                      component's exports
                                    properties:
                                      component:
                                        description: Component is the name of the
                                          component to reference
                                        type: string
                                      key:
                                        description: Key is the key within the component's
                                          exports
                                        type: string
                                    required:
                                    - component
                                    - key
                                    type: object
                                  secretKeyRef:
                                    description: SecretKeyRef selects a key of a secret
                                      in the pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        description: |-
                                          Name of the referent.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          TODO: Add other useful fields. apiVersion, kind, uid?
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        git:
                          description: Git repository configuration (required when
                            buildMode is dockerfile or buildpack)
                          properties:
                            contextPath:
                              default: .
                              description: ContextPath is the build context path
                              type: string
                            dockerfilePath:
                              default: Dockerfile
                              description: DockerfilePath is the path to the Dockerfile
                                (for dockerfile builds)
                              type: string
                            ref:
                              default: main
                              description: Ref is the git reference (branch, tag,
                                or commit)
                              type: string
                            repo:
                              description: Repo is the git repository URL
                              pattern: ^https?://.*\.git$|^git@.*:.*\.git$
                              type: string
                          required:
                          - ref
                          - repo
                          type: object
                        image:
                          description: Image is the container image to deploy (used
                            when buildMode is "image")
                          type: string
                        ingress:
                          description: Ingress configuration
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations for the ingress
                              type: object
                            enabled:
                              default: false
                              description: Enabled determines if ingress should be
                                created
                              type: boolean
                            host:
                              description: Host is the hostname for the ingress
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                              type: string
                            path:
                              default: /
                              description: Path is the path prefix for the ingress
                              type: string
                            tls:
                              description: TLS configuration for the ingress
                              properties:
                                hosts:
                                  description: Hosts is a list of hosts included in
                                    the TLS certificate
                                  items:
                                    type: string
                                  type: array
                                secretName:
                                  description: SecretName is the name of the secret
                                    containing TLS certificates
                                  type: string
                              type: object
                          type: object
                        port:
                          default: 8080
                          description: Port is the container port to expose
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        replicas:
                          default: 1
                          description: Replicas is the number of desired replicas
                          format: int32
                          maximum: 100
                          minimum: 0
                          type: integer
                        resources:
                          description: Resources defines resource requirements
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.


                                This is an alpha field and requires enabling the
                                DynamicResourceAllocation feature gate.


                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        values:
                          description: Values are custom Helm values to apply
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              parameters:
                description: Parameters are the parameters to pass to the stack template
                x-kubernetes-preserve-unknown-fields: true
              stackRef:
                description: StackRef references the stack template to deploy
                properties:
                  name:
                    description: Name is the name of the stack template
                    minLength: 1
                    type: string
                  version:
                    description: Version is the version of the stack template
                    minLength: 1
                    type: string
                required:
                - name
                - version
                type: object
              timeout:
                default: 15m
                description: Timeout for stack deployment operations
                type: string
            required:
            - components
            - stackRef
            type: object
          status:
            description: StackDeployStatus defines the observed state of StackDeploy
            properties:
              components:
                description: Components show the status of each component
                items:
                  description: ComponentStatus defines the status of a component within
                    a stack
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the phase transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional details about the component
                        status
                      type: string
                    name:
                      description: Name is the name of the component
                      type: string
                    phase:
                      description: Phase is the current phase of the component
                      type: string
                    ready:
                      description: Ready indicates if the component is ready
                      type: boolean
                    type:
                      description: Type is the type of the component
                      enum:
                      - workload
                      - addon
                      type: string
                  required:
                  - name
                  - phase
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed StackDeploy spec
                format: int64
                type: integer
              phase:
                default: Pending
                description: Phase represents the current phase of the stack deployment
                enum:
                - Pending
                - Deploying
                - Running
                - Degraded
                - Failed
                type: string
              readyComponents:
                description: ReadyComponents is the number of components that are
                  ready
                format: int32
                type: integer
              totalComponents:
                description: TotalComponents is the total number of components in
                  the stack
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
